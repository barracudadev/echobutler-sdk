name: Docs

on:
  pull_request:
    paths:
      - 'packages/js/**'
      - 'crates/**'
      - 'packages/flutter/**'
      - 'docs-site/**'
      - '.github/workflows/docs.yml'
  push:
    branches: [main]
    paths:
      - 'packages/js/**'
      - 'crates/**'
      - 'packages/flutter/**'
      - 'docs-site/**'
      - '.github/workflows/docs.yml'

jobs:
  build-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: '20'

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # stable (2026-08-05)
        with:
          toolchain: stable

      - name: Set up Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2.23.0
        with:
          channel: stable

      # JS/TS API reference (typedoc)
      - name: Install workspace dependencies
        run: npm install

      # The docs typecheck (docusaurus-plugin-typedoc) compiles the whole
      # packages/js tree against the root tsconfig — including
      # packages/js/wasm/test/wasm.test.ts, which imports the generated
      # dist/ entry point — so build @echobutler/wasm (tsc -> dist/) first.
      - name: Build JS packages
        run: |
          npm run build --workspace=@echobutler/wasm
          npm run build --workspace=@echobutler/analytics
          npm run build --workspace=@echobutler/core
          npm run build --workspace=@echobutler/mood
          npm run build --workspace=@echobutler/react
          npm run build --workspace=@echobutler/stellar

      - name: Verify WASM package output
        run: test -f packages/js/wasm/dist/index.js

      # Rust API reference (cargo doc)
      - name: Generate Rust docs
        run: cargo doc --workspace --no-deps

      # Dart API reference (dartdoc)
      - name: Install Flutter package dependencies
        working-directory: packages/flutter
        run: flutter pub get

      - name: Generate Dart docs
        working-directory: packages/flutter
        run: dart doc .

      # Wire generated docs into the Docusaurus static folder
      - name: Copy Rust docs into docs site
        run: |
          mkdir -p docs-site/static/api
          cp -r target/doc docs-site/static/api/rust

      - name: Copy Dart docs into docs site
        run: cp -r packages/flutter/doc/api docs-site/static/api/dart

      # Build the docs site — fails on typedoc errors, undocumented public
      # APIs surfaced as typedoc warnings-as-errors, or broken internal links
      # (onBrokenLinks: 'throw' in docusaurus.config.ts)
      - name: Install docs site dependencies
        working-directory: docs-site
        run: npm install

      # Aggregate all per-package CHANGELOG.md files into docs-site/docs/changelog.md
      # This must run before the Docusaurus build so the generated page is included.
      - name: Aggregate changelogs
        run: node docs-site/scripts/aggregate-changelogs.mjs

      - name: Build docs site
        working-directory: docs-site
        run: npm run build
