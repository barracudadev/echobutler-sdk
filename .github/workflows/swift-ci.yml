name: Swift SDK CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  swift-sdk:
    name: Build XCFramework and Test
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7
      - uses: maxim-lobanov/setup-xcode@ed7a3b1fda3918c0306d1b724322adc0b8cc0a90 # v1.7.0
        with:
          xcode-version: latest-stable
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # stable (2026-08-05)
        with:
          toolchain: stable
          targets: aarch64-apple-ios,x86_64-apple-ios,aarch64-apple-ios-sim,aarch64-apple-darwin,x86_64-apple-darwin
      - name: Check Swift toolchain
        run: |
          xcodebuild -version
          swift --version
      - name: Build EchoButlerFFI XCFramework
        run: bash packages/swift/EchoButlerSDK/Scripts/build-xcframework.sh
      - name: Run Swift tests
        run: swift test --package-path packages/swift/EchoButlerSDK
