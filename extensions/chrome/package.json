{
  "name": "@echobutler/chrome-extension",
  "private": true,
  "version": "0.1.0",
  "description": "EchoButler SDK Chrome extension — Stellar transaction monitor, mood widget injector, and SDK integration preview",
  "scripts": {
    "test": "vitest run --config vitest.config.ts",
    "coverage": "vitest run --coverage --config vitest.config.ts",
    "test:watch": "vitest watch --config vitest.config.ts",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.301",
    "@vitest/coverage-v8": "^3.2.7",
    "jsdom": "^24.1.3",
    "typescript": "^5.4.0",
    "vitest": "^3.2.7"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Echo-Mirror-Butler/echobutler-sdk"
  },
  "license": "MIT"
}
