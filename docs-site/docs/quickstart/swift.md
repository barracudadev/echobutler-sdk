---
sidebar_position: 6
---

# Swift Quickstart

:::caution Coming soon
`EchoButlerSDK` for iOS/macOS is not yet available. It will wrap [`echobutler-ffi`](pathname:///api/rust/echobutler_ffi/index.html), the same C-ABI shared library used by the Flutter SDK, and ship via Swift Package Manager.
:::

Track progress on the Swift package in the [EchoButler SDK issue tracker](https://github.com/karanjadavi/echobutler-sdk/issues). In the meantime:

- [Core Concepts](../core-concepts) explains the architecture the Swift SDK will slot into
- The [Flutter Quickstart](./flutter) shows the same FFI pattern (`dart:ffi`) that Swift will use (Swift FFI) against the same shared library

